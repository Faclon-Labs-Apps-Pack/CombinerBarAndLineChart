import type { GTPGlobalTimepicker, GTPChart, TimeTabUIConfig } from './types';
import './TimeTabConfiguration.css';
export interface TimeTabConfigurationProps {
    value?: Partial<TimeTabUIConfig>;
    onChange: (value: TimeTabUIConfig) => void;
    globalTimepickers?: GTPGlobalTimepicker[];
    /** When 'series', hides the Shift accordion and periodicity options. */
    mode?: string;
    /** Charts (and their data sources) in the host widget. Used to render the per-source
     *  deviation override section when Comparison Mode + Advance Settings are both on. */
    charts?: GTPChart[];
}
export declare function TimeTabConfiguration({ value, onChange, globalTimepickers, mode, charts }: TimeTabConfigurationProps): import("react/jsx-runtime").JSX.Element;
