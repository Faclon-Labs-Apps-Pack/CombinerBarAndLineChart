import { type SideNavBarContextValue } from './SideNavBarContext';
/**
 * Read the SideNavBar context value. Must be used inside a `<SideNavBarProvider>`.
 *
 * @example
 *   const { open, toggleSideNavBar } = useSideNavBar();
 */
export declare function useSideNavBar(): SideNavBarContextValue;
