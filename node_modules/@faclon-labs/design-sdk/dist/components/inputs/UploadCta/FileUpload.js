import { jsxs as w, jsx as o } from "react/jsx-runtime";
import { useState as A } from "react";
import { cn as G } from "../../../utils/cn.js";
import { InputFieldHeader as J } from "../../forms/InputFieldHeader/InputFieldHeader.js";
import { InputFieldFooter as K } from "../../forms/InputFieldFooter/InputFieldFooter.js";
import { UploadCta as L } from "./UploadCta.js";
import { UploadItem as Q } from "./UploadItem.js";
import { FilePreviewModal as V } from "./FilePreviewModal.js";
import { ensureFileId as W, triggerDownload as X, deriveFileTypeFromName as Y, formatFileSize as Z } from "./fileHelpers.js";
/* empty css               */
function $({
  uploadType: U,
  mode: x,
  label: u,
  necessityIndicator: M = "none",
  helpText: j,
  errorText: z,
  validationState: E = "none",
  isDisabled: S = !1,
  accept: _,
  maxSize: l,
  maxCount: i,
  files: n = [],
  onFilesSelect: p,
  onRemove: m,
  onDownload: a,
  onPreview: f,
  onReupload: c,
  disableBuiltInPreview: h = !1,
  disableBuiltInDownload: k = !1,
  className: C,
  ...H
}) {
  const d = U ?? x ?? "single", g = E === "error", F = g ? z : j, [y, T] = A(null), O = d === "multiple" && typeof i == "number" && n.length >= i, I = !(d === "single" && n.length > 0) && !O, q = I && !!F, v = (e) => {
    if (!p) return;
    if (typeof l != "number" && typeof i != "number") {
      p(e);
      return;
    }
    const t = [], r = typeof i == "number" ? Math.max(0, i - n.length) : 1 / 0;
    for (let s = 0; s < e.length; s++) {
      const b = e[s];
      if (!(typeof l == "number" && b.size > l)) {
        if (t.length >= r) break;
        t.push(b);
      }
    }
    if (t.length === 0) return;
    const N = new DataTransfer();
    t.forEach((s) => N.items.add(s)), p(N.files);
  };
  return /* @__PURE__ */ w("div", { className: G("fds-file-upload", C), ...H, children: [
    u && /* @__PURE__ */ o(J, { label: u, necessityIndicator: M }),
    I && /* @__PURE__ */ o(
      L,
      {
        isDisabled: S,
        accept: _,
        multiple: d === "multiple",
        onFilesSelect: v
      }
    ),
    q && /* @__PURE__ */ o(
      K,
      {
        helpText: F,
        state: g ? "error" : "default"
      }
    ),
    n.length > 0 && /* @__PURE__ */ o("div", { className: "fds-file-upload__list", children: n.map((e, t) => {
      const r = W(e, t);
      return /* @__PURE__ */ o(
        Q,
        {
          fileName: e.name,
          fileSize: Z(e.size),
          fileType: e.fileIcon ?? Y(e.name),
          uploadState: e.state,
          progress: e.progress,
          errorText: e.errorText,
          onRemove: () => m == null ? void 0 : m(r),
          onDownload: () => {
            a == null || a(r), k || X(e);
          },
          onPreview: () => {
            f == null || f(r), h || T(e);
          },
          onRetry: () => c == null ? void 0 : c(r)
        },
        r
      );
    }) }),
    !h && /* @__PURE__ */ o(
      V,
      {
        file: y,
        isOpen: y !== null,
        onClose: () => T(null)
      }
    )
  ] });
}
$.displayName = "FileUpload";
export {
  $ as FileUpload
};
