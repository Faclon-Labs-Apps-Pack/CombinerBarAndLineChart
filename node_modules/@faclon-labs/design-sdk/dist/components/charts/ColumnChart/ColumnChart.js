import { jsxs as F, Fragment as D, jsx as a } from "react/jsx-runtime";
import { forwardRef as G, useRef as H, useEffect as J, useMemo as K } from "react";
import j from "highcharts";
import O from "highcharts-react-official";
import { Chart as M } from "../Chart/Chart.js";
import { useFaclonChartTheme as Q, readCssVar as P } from "../Chart/highchartsTheme.js";
/* empty css                */
const X = O.default ?? O, Y = G(
  ({
    series: f,
    categories: _,
    stacked: A = !1,
    showLegend: d = !0,
    showDataLabels: N = !1,
    dataLabelFormat: u,
    onPointClick: h,
    colors: g,
    xAxisTitle: x,
    yAxisTitle: b,
    yAxisUnit: v,
    plotLines: c,
    plotBands: i,
    zoomable: z = !0,
    scrollable: m = !1,
    scrollableMinWidth: I,
    highchartsOptions: y,
    bare: q = !1,
    onChartReady: p,
    ...V
  }, W) => {
    const l = Q(), S = H(null), w = H(null);
    J(() => {
      const r = w.current;
      if (!r || typeof ResizeObserver > "u") return;
      let t = 0, o = !0;
      const n = new ResizeObserver(() => {
        if (o) {
          o = !1;
          return;
        }
        cancelAnimationFrame(t), t = requestAnimationFrame(() => {
          var s;
          return (s = S.current) == null ? void 0 : s.reflow();
        });
      });
      return n.observe(r), () => {
        cancelAnimationFrame(t), n.disconnect();
      };
    }, []);
    const $ = K(() => {
      var s, k, E;
      const r = f.map((e) => ({
        type: "column",
        name: e.name,
        data: e.data,
        color: e.color
      })), t = i == null ? void 0 : i.map((e) => ({
        from: e.from,
        to: e.to,
        color: e.color ?? "rgba(239,68,68,0.1)",
        zIndex: e.zIndex ?? 0,
        ...e.label && {
          label: {
            text: e.label,
            align: e.labelAlign ?? "right"
          }
        }
      })), o = c == null ? void 0 : c.map((e) => ({
        value: e.value,
        color: (e.color ?? P("--border-error-default")) || "#ef4444",
        width: e.width ?? 2,
        dashStyle: e.dashStyle ?? "Dash",
        zIndex: e.zIndex ?? 5,
        ...e.label && {
          label: {
            text: e.label,
            align: e.labelAlign ?? "right",
            style: { color: (e.color ?? P("--border-error-default")) || "#ef4444" }
          }
        }
      })), n = {
        ...l,
        ...g && { colors: g },
        chart: {
          ...l.chart,
          type: "column",
          ...z && { zooming: { type: "x", singleTouch: !0, mouseWheel: !1 } },
          ...m && { scrollablePlotArea: { minWidth: I ?? 800, opacity: 1 } }
        },
        xAxis: {
          ...l.xAxis,
          categories: _,
          ...x !== void 0 && { title: { ...(s = l.xAxis) == null ? void 0 : s.title, text: x } }
        },
        yAxis: {
          ...l.yAxis,
          ...b !== void 0 && { title: { ...(k = l.yAxis) == null ? void 0 : k.title, text: b } },
          ...v && { labels: { ...(E = l.yAxis) == null ? void 0 : E.labels, format: `{value} ${v}` } },
          ...o && { plotLines: o },
          ...t && { plotBands: t }
        },
        plotOptions: {
          column: {
            stacking: A ? "normal" : void 0,
            dataLabels: {
              enabled: N,
              ...u && { format: u },
              // Show every label even when they would visually collide;
              // consumers can opt back into Highcharts' overlap-dodging
              // via `highchartsOptions.plotOptions.column.dataLabels`.
              allowOverlap: !0
            },
            ...h && {
              cursor: "pointer",
              point: {
                events: {
                  click() {
                    h({
                      category: String(this.category ?? ""),
                      seriesName: this.series.name,
                      value: this.y ?? null,
                      pointIndex: this.index,
                      seriesIndex: this.series.index
                    });
                  }
                }
              }
            }
          }
        },
        legend: {
          ...l.legend,
          enabled: m ? !1 : d
        },
        // Export config: hide the built-in hamburger (SDK ships its own
        // menu), force offline-only (no POST to export.highcharts.com),
        // and inline CSS into the export SVG so legend / axis text render.
        exporting: { enabled: !1, fallbackToExportServer: !1, applyStyleSheets: !0 },
        series: r
      };
      return y ? j.merge(n, y) : n;
    }, [l, f, _, A, d, N, u, z, m, I, h, g, x, b, v, c, i, y]), C = l.colors ?? [], R = /* @__PURE__ */ F(D, { children: [
      /* @__PURE__ */ a("div", { ref: w, style: { width: "100%", height: "100%" }, children: /* @__PURE__ */ a(
        X,
        {
          highcharts: j,
          options: $,
          callback: (r) => {
            var t;
            (t = r.options.chart) != null && t.forExport || (S.current = r, p == null || p(r));
          },
          containerProps: { className: "fds-column-chart" }
        }
      ) }),
      m && d && /* @__PURE__ */ a("div", { className: "fds-chart__scrollable-legend", children: f.map((r, t) => /* @__PURE__ */ F("span", { className: "fds-chart__scrollable-legend-item", children: [
        /* @__PURE__ */ a(
          "span",
          {
            className: "fds-chart__scrollable-legend-symbol",
            style: { backgroundColor: r.color ?? C[t % C.length] }
          }
        ),
        /* @__PURE__ */ a("span", { className: "fds-chart__scrollable-legend-label", children: r.name })
      ] }, r.name)) })
    ] });
    return q ? R : /* @__PURE__ */ a(M, { ref: W, ...V, children: R });
  }
);
Y.displayName = "ColumnChart";
export {
  Y as ColumnChart
};
