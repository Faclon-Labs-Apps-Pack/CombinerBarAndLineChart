import { jsxs as z, Fragment as Q, jsx as i } from "react/jsx-runtime";
import { forwardRef as T, useRef as W, useEffect as X, useMemo as Y } from "react";
import q from "highcharts";
import F from "highcharts-react-official";
import { Chart as Z } from "../Chart/Chart.js";
import { useFaclonChartTheme as D, readCssVar as V } from "../Chart/highchartsTheme.js";
/* empty css              */
const B = F.default ?? F, M = T(
  ({
    series: m,
    categories: I,
    smooth: N = !1,
    showMarkers: u,
    showLegend: h = !0,
    showDataLabels: x = !1,
    dataLabelFormat: a,
    onPointClick: g,
    colors: v,
    xAxisTitle: p,
    yAxisTitle: y,
    yAxisUnit: b,
    plotLines: c,
    plotBands: f,
    zoomable: _ = !0,
    scrollable: d = !1,
    scrollableMinWidth: k,
    highchartsOptions: A,
    bare: $ = !1,
    onChartReady: S,
    ...G
  }, J) => {
    const t = D(), w = W(null), R = W(null);
    X(() => {
      const r = R.current;
      if (!r || typeof ResizeObserver > "u") return;
      let l = 0, n = !0;
      const o = new ResizeObserver(() => {
        if (n) {
          n = !1;
          return;
        }
        cancelAnimationFrame(l), l = requestAnimationFrame(() => {
          var s;
          return (s = w.current) == null ? void 0 : s.reflow();
        });
      });
      return o.observe(r), () => {
        cancelAnimationFrame(l), o.disconnect();
      };
    }, []);
    const K = Y(() => {
      var H, j, P;
      const r = N ? "spline" : "line", l = m.map((e) => ({
        type: r,
        name: e.name,
        data: e.data,
        color: e.color,
        ...u !== void 0 && { marker: { enabled: u } }
      })), n = f == null ? void 0 : f.map((e) => ({
        from: e.from,
        to: e.to,
        color: e.color ?? "rgba(239,68,68,0.1)",
        zIndex: e.zIndex ?? 0,
        ...e.label && {
          label: { text: e.label, align: e.labelAlign ?? "right" }
        }
      })), o = c == null ? void 0 : c.map((e) => ({
        value: e.value,
        color: (e.color ?? V("--border-error-default")) || "#ef4444",
        width: e.width ?? 2,
        dashStyle: e.dashStyle ?? "Dash",
        zIndex: e.zIndex ?? 5,
        ...e.label && {
          label: {
            text: e.label,
            align: e.labelAlign ?? "right",
            style: { color: (e.color ?? V("--border-error-default")) || "#ef4444" }
          }
        }
      })), s = g ? {
        cursor: "pointer",
        point: {
          events: {
            click() {
              g({
                category: String(this.category ?? ""),
                seriesName: this.series.name,
                value: this.y ?? null,
                pointIndex: this.index,
                seriesIndex: this.series.index
              });
            }
          }
        }
      } : {}, E = {
        ...t,
        ...v && { colors: v },
        chart: {
          ...t.chart,
          type: r,
          ..._ && { zooming: { type: "x", singleTouch: !0, mouseWheel: !1 } },
          ...d && { scrollablePlotArea: { minWidth: k ?? 800, opacity: 1 } }
        },
        xAxis: {
          ...t.xAxis,
          categories: I,
          ...p !== void 0 && { title: { ...(H = t.xAxis) == null ? void 0 : H.title, text: p } }
        },
        yAxis: {
          ...t.yAxis,
          ...y !== void 0 && { title: { ...(j = t.yAxis) == null ? void 0 : j.title, text: y } },
          ...b && { labels: { ...(P = t.yAxis) == null ? void 0 : P.labels, format: `{value} ${b}` } },
          ...o && { plotLines: o },
          ...n && { plotBands: n }
        },
        plotOptions: {
          line: {
            dataLabels: {
              enabled: x,
              ...a && { format: a },
              allowOverlap: !0
            },
            ...s
          },
          spline: {
            dataLabels: {
              enabled: x,
              ...a && { format: a },
              allowOverlap: !0
            },
            ...s
          }
        },
        legend: {
          ...t.legend,
          enabled: d ? !1 : h
        },
        // Hide Highcharts' built-in hamburger; we render our own export menu.
        // Export config: hide the built-in hamburger, force offline-only
        // (no POST to export.highcharts.com), and inline CSS into the
        // export SVG so legend / axis text render.
        exporting: { enabled: !1, fallbackToExportServer: !1, applyStyleSheets: !0 },
        series: l
      };
      return A ? q.merge(E, A) : E;
    }, [t, m, I, N, u, h, x, a, _, d, k, g, v, p, y, b, c, f, A]), O = t.colors ?? [], C = /* @__PURE__ */ z(Q, { children: [
      /* @__PURE__ */ i("div", { ref: R, style: { width: "100%", height: "100%" }, children: /* @__PURE__ */ i(
        B,
        {
          highcharts: q,
          options: K,
          callback: (r) => {
            var l;
            (l = r.options.chart) != null && l.forExport || (w.current = r, S == null || S(r));
          },
          containerProps: { className: "fds-line-chart" }
        }
      ) }),
      d && h && /* @__PURE__ */ i("div", { className: "fds-chart__scrollable-legend", children: m.map((r, l) => {
        const n = r.color ?? O[l % O.length];
        return /* @__PURE__ */ z("span", { className: "fds-chart__scrollable-legend-item", children: [
          /* @__PURE__ */ z("svg", { width: "16", height: "12", viewBox: "0 0 16 12", "aria-hidden": "true", style: { flexShrink: 0 }, children: [
            /* @__PURE__ */ i("line", { x1: "0", y1: "6", x2: "16", y2: "6", stroke: n, strokeWidth: "2" }),
            /* @__PURE__ */ i("circle", { cx: "8", cy: "6", r: "3", fill: n })
          ] }),
          /* @__PURE__ */ i("span", { className: "fds-chart__scrollable-legend-label", children: r.name })
        ] }, r.name);
      }) })
    ] });
    return $ ? C : /* @__PURE__ */ i(Z, { ref: J, ...G, children: C });
  }
);
M.displayName = "LineChart";
export {
  M as LineChart
};
