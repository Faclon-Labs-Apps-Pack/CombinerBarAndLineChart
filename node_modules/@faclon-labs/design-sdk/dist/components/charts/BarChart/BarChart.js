import { jsx as g } from "react/jsx-runtime";
import { forwardRef as M, useRef as H, useEffect as P, useMemo as V } from "react";
import N from "highcharts";
import k from "highcharts-react-official";
import { Chart as $ } from "../Chart/Chart.js";
import { useFaclonChartTheme as D, readCssVar as F } from "../Chart/highchartsTheme.js";
/* empty css             */
const G = k.default ?? k, J = M(
  ({
    series: v,
    categories: y,
    stacked: p = !1,
    showLegend: w = !0,
    showDataLabels: z = !1,
    dataLabelFormat: c,
    onPointClick: f,
    colors: m,
    xAxisTitle: u,
    yAxisTitle: d,
    yAxisUnit: h,
    plotLines: i,
    plotBands: s,
    highchartsOptions: x,
    bare: O = !1,
    onChartReady: b,
    ...j
  }, q) => {
    const r = D(), A = H(null), I = H(null);
    P(() => {
      const a = I.current;
      if (!a || typeof ResizeObserver > "u") return;
      let t = 0, l = !0;
      const o = new ResizeObserver(() => {
        if (l) {
          l = !1;
          return;
        }
        cancelAnimationFrame(t), t = requestAnimationFrame(() => {
          var n;
          return (n = A.current) == null ? void 0 : n.reflow();
        });
      });
      return o.observe(a), () => {
        cancelAnimationFrame(t), o.disconnect();
      };
    }, []);
    const C = V(() => {
      var n, R, E;
      const a = v.map((e) => ({
        type: "bar",
        name: e.name,
        data: e.data,
        color: e.color
      })), t = s == null ? void 0 : s.map((e) => ({
        from: e.from,
        to: e.to,
        color: e.color ?? "rgba(239,68,68,0.1)",
        zIndex: e.zIndex ?? 0,
        ...e.label && {
          label: { text: e.label, align: e.labelAlign ?? "right" }
        }
      })), l = i == null ? void 0 : i.map((e) => ({
        value: e.value,
        color: (e.color ?? F("--border-error-default")) || "#ef4444",
        width: e.width ?? 2,
        dashStyle: e.dashStyle ?? "Dash",
        zIndex: e.zIndex ?? 5,
        ...e.label && {
          label: {
            text: e.label,
            align: e.labelAlign ?? "right",
            style: { color: (e.color ?? F("--border-error-default")) || "#ef4444" }
          }
        }
      })), o = {
        ...r,
        ...m && { colors: m },
        chart: {
          ...r.chart,
          type: "bar"
        },
        xAxis: {
          ...r.xAxis,
          categories: y,
          ...u !== void 0 && { title: { ...(n = r.xAxis) == null ? void 0 : n.title, text: u } }
        },
        yAxis: {
          ...r.yAxis,
          ...d !== void 0 && { title: { ...(R = r.yAxis) == null ? void 0 : R.title, text: d } },
          ...h && { labels: { ...(E = r.yAxis) == null ? void 0 : E.labels, format: `{value} ${h}` } },
          ...l && { plotLines: l },
          ...t && { plotBands: t }
        },
        plotOptions: {
          bar: {
            stacking: p ? "normal" : void 0,
            dataLabels: {
              enabled: z,
              ...c && { format: c },
              allowOverlap: !0
            },
            ...f && {
              cursor: "pointer",
              point: {
                events: {
                  click() {
                    f({
                      category: String(this.category ?? ""),
                      seriesName: this.series.name,
                      value: this.y ?? null,
                      pointIndex: this.index,
                      seriesIndex: this.series.index
                    });
                  }
                }
              }
            }
          }
        },
        legend: {
          ...r.legend,
          enabled: w
        },
        // Hide Highcharts' built-in hamburger; we render our own export menu.
        // Export config: hide the built-in hamburger, force offline-only
        // (no POST to export.highcharts.com), and inline CSS into the
        // export SVG so legend / axis text render.
        exporting: { enabled: !1, fallbackToExportServer: !1, applyStyleSheets: !0 },
        series: a
      };
      return x ? N.merge(o, x) : o;
    }, [r, v, y, p, w, z, c, f, m, u, d, h, i, s, x]), S = /* @__PURE__ */ g("div", { ref: I, style: { width: "100%", height: "100%" }, children: /* @__PURE__ */ g(
      G,
      {
        highcharts: N,
        options: C,
        callback: (a) => {
          var t;
          (t = a.options.chart) != null && t.forExport || (A.current = a, b == null || b(a));
        },
        containerProps: { className: "fds-bar-chart" }
      }
    ) });
    return O ? S : /* @__PURE__ */ g($, { ref: q, ...j, children: S });
  }
);
J.displayName = "BarChart";
export {
  J as BarChart
};
