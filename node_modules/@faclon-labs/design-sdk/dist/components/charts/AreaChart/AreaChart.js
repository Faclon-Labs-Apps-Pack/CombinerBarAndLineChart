import { jsxs as V, Fragment as Y, jsx as l } from "react/jsx-runtime";
import { forwardRef as Z, useRef as W, useEffect as D, useMemo as M } from "react";
import $ from "highcharts";
import G from "highcharts-react-official";
import { Chart as U } from "../Chart/Chart.js";
import { useFaclonChartTheme as B, readCssVar as J } from "../Chart/highchartsTheme.js";
/* empty css              */
const L = G.default ?? G, ee = Z(
  ({
    series: d,
    categories: N,
    stacked: z = !1,
    percentStacked: I = !1,
    smooth: R = !1,
    showMarkers: u,
    showLegend: h = !0,
    showDataLabels: g = !1,
    dataLabelFormat: n,
    onPointClick: x,
    colors: p,
    xAxisTitle: v,
    yAxisTitle: b,
    yAxisUnit: y,
    plotLines: c,
    plotBands: i,
    zoomable: S = !0,
    scrollable: f = !1,
    scrollableMinWidth: k,
    highchartsOptions: A,
    bare: K = !1,
    onChartReady: _,
    ...Q
  }, T) => {
    const a = B(), C = W(null), O = W(null);
    D(() => {
      const r = O.current;
      if (!r || typeof ResizeObserver > "u") return;
      let t = 0, m = !0;
      const o = new ResizeObserver(() => {
        if (m) {
          m = !1;
          return;
        }
        cancelAnimationFrame(t), t = requestAnimationFrame(() => {
          var s;
          return (s = C.current) == null ? void 0 : s.reflow();
        });
      });
      return o.observe(r), () => {
        cancelAnimationFrame(t), o.disconnect();
      };
    }, []);
    const X = M(() => {
      var P, q, F;
      const r = R ? "areaspline" : "area", t = I ? "percent" : z ? "normal" : void 0, m = d.map((e) => ({
        type: r,
        name: e.name,
        data: e.data,
        color: e.color,
        ...u !== void 0 && { marker: { enabled: u } }
      })), o = i == null ? void 0 : i.map((e) => ({
        from: e.from,
        to: e.to,
        color: e.color ?? "rgba(239,68,68,0.1)",
        zIndex: e.zIndex ?? 0,
        ...e.label && {
          label: { text: e.label, align: e.labelAlign ?? "right" }
        }
      })), s = c == null ? void 0 : c.map((e) => ({
        value: e.value,
        color: (e.color ?? J("--border-error-default")) || "#ef4444",
        width: e.width ?? 2,
        dashStyle: e.dashStyle ?? "Dash",
        zIndex: e.zIndex ?? 5,
        ...e.label && {
          label: {
            text: e.label,
            align: e.labelAlign ?? "right",
            style: { color: (e.color ?? J("--border-error-default")) || "#ef4444" }
          }
        }
      })), H = x ? {
        cursor: "pointer",
        point: {
          events: {
            click() {
              x({
                category: String(this.category ?? ""),
                seriesName: this.series.name,
                value: this.y ?? null,
                pointIndex: this.index,
                seriesIndex: this.series.index
              });
            }
          }
        }
      } : {}, j = {
        ...a,
        ...p && { colors: p },
        chart: {
          ...a.chart,
          type: r,
          ...S && { zooming: { type: "x", singleTouch: !0, mouseWheel: !1 } },
          ...f && { scrollablePlotArea: { minWidth: k ?? 800, opacity: 1 } }
        },
        xAxis: {
          ...a.xAxis,
          categories: N,
          ...v !== void 0 && { title: { ...(P = a.xAxis) == null ? void 0 : P.title, text: v } }
        },
        yAxis: {
          ...a.yAxis,
          ...b !== void 0 && { title: { ...(q = a.yAxis) == null ? void 0 : q.title, text: b } },
          ...y && { labels: { ...(F = a.yAxis) == null ? void 0 : F.labels, format: `{value} ${y}` } },
          ...s && { plotLines: s },
          ...o && { plotBands: o }
        },
        plotOptions: {
          area: {
            stacking: t,
            dataLabels: {
              enabled: g,
              ...n && { format: n },
              allowOverlap: !0
            },
            ...H
          },
          areaspline: {
            stacking: t,
            dataLabels: {
              enabled: g,
              ...n && { format: n },
              allowOverlap: !0
            },
            ...H
          }
        },
        legend: {
          ...a.legend,
          enabled: f ? !1 : h
        },
        // Hide Highcharts' built-in hamburger; we render our own export menu.
        // Export config: hide the built-in hamburger, force offline-only
        // (no POST to export.highcharts.com), and inline CSS into the
        // export SVG so legend / axis text render.
        exporting: { enabled: !1, fallbackToExportServer: !1, applyStyleSheets: !0 },
        series: m
      };
      return A ? $.merge(j, A) : j;
    }, [a, d, N, z, I, R, u, h, g, n, S, f, k, x, p, v, b, y, c, i, A]), w = a.colors ?? [], E = /* @__PURE__ */ V(Y, { children: [
      /* @__PURE__ */ l("div", { ref: O, style: { width: "100%", height: "100%" }, children: /* @__PURE__ */ l(
        L,
        {
          highcharts: $,
          options: X,
          callback: (r) => {
            var t;
            (t = r.options.chart) != null && t.forExport || (C.current = r, _ == null || _(r));
          },
          containerProps: { className: "fds-area-chart" }
        }
      ) }),
      f && h && /* @__PURE__ */ l("div", { className: "fds-chart__scrollable-legend", children: d.map((r, t) => /* @__PURE__ */ V("span", { className: "fds-chart__scrollable-legend-item", children: [
        /* @__PURE__ */ l(
          "span",
          {
            className: "fds-chart__scrollable-legend-symbol",
            style: { backgroundColor: r.color ?? w[t % w.length] }
          }
        ),
        /* @__PURE__ */ l("span", { className: "fds-chart__scrollable-legend-label", children: r.name })
      ] }, r.name)) })
    ] });
    return K ? E : /* @__PURE__ */ l(U, { ref: T, ...Q, children: E });
  }
);
ee.displayName = "AreaChart";
export {
  ee as AreaChart
};
