import { jsx as c } from "react/jsx-runtime";
import { forwardRef as k, useRef as f, useEffect as w, useMemo as T } from "react";
import E from "highcharts/esm/highcharts.js";
import "highcharts/esm/highcharts-more.js";
import "highcharts/esm/modules/solid-gauge.js";
import "../../../node_modules/.pnpm/highcharts@12.5.0/node_modules/highcharts/esm/modules/exporting.js";
import "../../../node_modules/.pnpm/highcharts@12.5.0/node_modules/highcharts/esm/modules/export-data.js";
import g from "highcharts-react-official";
import { Chart as _ } from "../Chart/Chart.js";
import { useFaclonChartTheme as N, FACLON_CHART_PALETTE_TOKENS as h } from "../Chart/highchartsTheme.js";
/* empty css                  */
const S = g.default ?? g;
function v(e, a) {
  return typeof window > "u" ? a : getComputedStyle(document.documentElement).getPropertyValue(e).trim() || a;
}
const y = 24, C = 1, F = y + C, u = (e) => 100 - e * F, b = (e) => u(e) - y, G = k(
  ({
    activities: e,
    showLegend: a = !0,
    highchartsOptions: m,
    onChartReady: l,
    ...R
  }, A) => {
    const i = N(), d = f(null), p = f(null);
    w(() => {
      const n = p.current;
      if (!n || typeof ResizeObserver > "u") return;
      let o = 0, t = !0;
      const r = new ResizeObserver(() => {
        if (t) {
          t = !1;
          return;
        }
        cancelAnimationFrame(o), o = requestAnimationFrame(() => {
          var s;
          return (s = d.current) == null ? void 0 : s.reflow();
        });
      });
      return r.observe(n), () => {
        cancelAnimationFrame(o), r.disconnect();
      };
    }, []);
    const x = T(() => {
      if (!e || e.length === 0)
        return { ...i, chart: { type: "solidgauge" } };
      const n = e.map((t, r) => {
        const s = h[r % h.length];
        return v(s, "#e9690c");
      }), o = n.map(
        (t) => `color-mix(in srgb, ${t} 30%, transparent)`
      );
      return {
        ...i,
        chart: {
          ...i.chart,
          type: "solidgauge"
        },
        tooltip: {
          backgroundColor: "none",
          borderWidth: 0,
          shadow: !1,
          fixed: !0,
          pointFormat: '{series.name}<br><span style="font-size: 2em; color: {point.color}; font-weight: bold">{point.y}</span>',
          position: {
            align: "center",
            verticalAlign: "middle"
          },
          valueSuffix: "%"
        },
        pane: {
          startAngle: 0,
          endAngle: 360,
          background: e.map((t, r) => ({
            outerRadius: `${u(r)}%`,
            innerRadius: `${b(r)}%`,
            backgroundColor: o[r],
            borderWidth: 0
          }))
        },
        yAxis: {
          min: 0,
          max: 100,
          lineWidth: 0,
          tickPositions: []
        },
        plotOptions: {
          solidgauge: {
            dataLabels: { enabled: !1 },
            linecap: "round",
            stickyTracking: !1,
            rounded: !0
          }
        },
        legend: {
          ...i.legend,
          enabled: a
        },
        // Hide Highcharts' built-in hamburger; we render our own export menu.
        // Export config: hide the built-in hamburger, force offline-only
        // (no POST to export.highcharts.com), and inline CSS into the
        // export SVG so labels render.
        exporting: { enabled: !1, fallbackToExportServer: !1, applyStyleSheets: !0 },
        series: e.map((t, r) => ({
          type: "solidgauge",
          name: t.name,
          showInLegend: !0,
          data: [
            {
              color: n[r],
              radius: `${u(r)}%`,
              innerRadius: `${b(r)}%`,
              y: Math.min(
                100,
                Math.max(0, t.value / (t.max ?? 100) * 100)
              )
            }
          ]
        })),
        ...m
      };
    }, [e, i, a, m]);
    return /* @__PURE__ */ c(_, { ref: A, ...R, children: /* @__PURE__ */ c("div", { ref: p, className: "fds-activity-gauge", children: /* @__PURE__ */ c(
      S,
      {
        highcharts: E,
        options: x,
        callback: (n) => {
          var o;
          (o = n.options.chart) != null && o.forExport || (d.current = n, l == null || l(n));
        },
        containerProps: { style: { width: "100%", height: "100%" } }
      }
    ) }) });
  }
);
G.displayName = "ActivityGauge";
export {
  G as ActivityGauge
};
